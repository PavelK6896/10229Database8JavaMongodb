server:
  port: 8080
  servlet:
    context-path:

spring:
  application:
    name: database8
  data:
    mongodb:
      host: localhost
      port: 27017
      database: ${DATABASE:database8}
  jackson:
    serialization:
      indent_output: true
  mongodb:
    embedded:
      version: 3.5.5

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
