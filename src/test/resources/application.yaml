server:
  port: 8080
  servlet:
    context-path:

spring:
  application:
    name: database8
  mongodb:
    embedded:
      version: 3.5.5
  jackson:
    serialization:
      indent_output: true

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
